# klafreniereadams.github.io

The LyricLSTM project was orignally run as a Jupyter Notebook .ipynb project, but the code here should be run as a .py file.
There is some repetition with the import statements, as running the code in Jupyter led to a lot of fragility, with dependencies being lost cell to cell. 
